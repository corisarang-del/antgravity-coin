# 프롬프트 기록 - planning 실패 정책과 계약 보강

- 작성시각: 2026-03-16 21:05:00 +09:00
- 해결하고자 한 문제:
  - 구현 전 문서가 실제 지침 역할을 하도록 실패 모드, 데이터 출처, 이벤트 계약, 캐시/버전 정책을 더 구체화할 필요가 있었음.
- 사용자 요청 요약:
  - `docs/planning` 문서에 실패 정책, 계약, 운영 규칙을 정책 수준으로 추가 보강해달라는 요청.
- 이번 작업에서 고정한 주요 정책:
  - `/api/characters` 정식 응답 shape는 `{ characters: [...] }`
  - `external` 캐릭터 소스 실패 시 로컬 fallback 허용
  - 배틀 SSE 중간 실패 시 `error` 이벤트 송신 후 스트림 종료
  - `BattleResult.ruleVersion`과 `BattleSnapshot.version`을 문서상 도입
  - `fetchMarketData` 필드별 출처를 매트릭스로 분리
- 해결된 것:
  - planning 문서만 읽어도 실패 처리와 계약 방향을 이해할 수 있게 됨
  - 후속 구현에서 먼저 맞춰야 할 정책 포인트가 문서에 고정됨
- 해결되지 않은 것:
  - 실제 코드와 테스트는 아직 이 정책을 반영하지 않았음

