# NewsAPI 키 연결

- 작성시각: 2026-03-18 19:40 KST
- 해결하고자 한 문제:
  - 전달받은 `NewsAPI` 키를 로컬 환경에 연결해서 라이브 테스트가 실제로 실행되게 해야 했다.

## 해결된 것

- `.env.local`에 `NEWS_API_KEY`를 추가했다.
- 라이브 테스트에서 `NewsAPI`가 skip이 아니라 실제로 호출되는지 확인할 준비를 마쳤다.

## 아직 안 된 것

- 키 유효성과 호출 성공 여부는 라이브 테스트 실행 결과로 최종 확인해야 한다.

