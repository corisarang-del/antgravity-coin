# 헤더 홈 진입점을 랜딩으로 복구

- 작성시각: 2026-03-28 21:08 KST

## 해결하고자 한 문제

- 상단 `홈` 버튼과 좌측 상단 `Ant Gravity` 로고를 눌렀을 때 시즌1 라인업이 있는 랜딩이 아니라 곧바로 배틀 화면으로 이동해서 사용자 흐름이 어색했음

## 해결된 것

- `src/presentation/components/AppHeader.tsx`에서 헤더 로고 링크를 `/home`에서 `/`로 변경했음
- 같은 파일의 `홈` 네비게이션 링크도 `/home`에서 `/`로 변경했음
- 이제 배틀/캐릭터도감/내 페이지 등 AppHeader가 붙는 화면에서 홈이나 로고를 누르면 시즌1 라인업 랜딩으로 돌아감

## 아직 안 된 것

- 별도 브라우저 E2E 검증은 아직 안 했음
- 랜딩에서 다시 배틀로 들어가는 CTA 문구/동선까지는 이번 수정 범위에 포함하지 않았음
