# 홈 헤드라인 한 줄 정리와 추천 코인 교체

- 작성시각: 2026-03-23 22:49 KST
- 해결하고자 한 문제:
  - 홈 상단의 `불리시팀 vs 베어리시팀`과 검색 카드의 `어떤 코인으로 붙어볼지 골라줘`가 줄바꿈되며 답답하게 보였음
  - 추천 코인 목록에 덜 유명한 코인 대신 더 대중적인 코인을 노출하고 싶었음

## 해결된 것

- `src/app/home/page.tsx`
  - `불리시팀 vs 베어리시팀`에 `whitespace-nowrap`와 더 보수적인 clamp 값을 적용해 한 줄로 보이도록 조정했음
- `src/presentation/components/SearchBar.tsx`
  - `어떤 코인으로 붙어볼지 골라줘`에도 `whitespace-nowrap`와 조정된 크기를 적용해 한 줄 가독성을 높였음
- `src/shared/constants/mockCoins.ts`
  - 현재 목록에 `FIGR_HELOC`는 없어서, 상대적으로 덜 대중적인 `TON` 추천 슬롯을 `AVAX`로 교체했음
  - 코인 상수 파일 인코딩이 깨져 있어 전체 파일을 UTF-8 기준으로 다시 정리했음
- 검증
  - `pnpm.cmd typecheck` 통과
  - 대상 파일 `eslint` 통과

## 해결되지 않은 것

- 실제 브라우저에서 아주 작은 모바일 폭까지 한 줄 유지가 완벽한지는 이번 턴에서 따로 시각 검수하지 않았음
- 추천 코인 구성 자체를 더 바꿀지는 추가 피드백에 따라 조정 가능함
