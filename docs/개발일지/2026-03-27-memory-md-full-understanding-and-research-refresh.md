# memory.md 완전 재이해와 research.md 보강

- 작성시각: 2026-03-27 12:53 KST
- 해결하고자 한 문제:
  - `memory.md`를 부분 발췌가 아니라 전체 운영 기준 문서로 다시 이해하고, 그 이해를 `research.md`에 빠짐없이 이어붙여 다음 세션 기준 문서로 보강할 필요가 있었음

## 해결된 것

- `memory.md`를 처음부터 끝까지 다시 읽고 아래 운영 판단을 다시 확정했음
  - 이 프로젝트의 핵심 문제는 무료 모델 조합의 불안정성을 UX와 운영 로직으로 흡수하는 일임
  - 체감 속도 기준은 첫 발언보다 `pick-ready` 시점임
  - 원격 반영본, local 미커밋 WIP, 런타임 산출물을 분리해서 읽어야 함
  - 다음 세션 첫 작업은 구현보다 `judy` / `shade` 검증과 모델 배치 재확인임
- `research.md` 상단 메타 정보에 최신 업데이트 시각을 반영했음
- `research.md`에 아래 보강 섹션을 추가했음
  - 문서 기록도 구현 일부라는 점
  - 세션 공통 작업 원칙
  - 현재 워크트리 분리 원칙
  - 지금 시점의 실무 우선 판단

## 해결되지 않은 것

- `memory.md`에 적힌 local WIP를 실제로 검증한 건 아님
- `/api/battle` live 재실측이나 `judy` / `shade` round 2 진행 확인은 아직 안 했음
- production 쪽 migration 적용 여부와 admin 권한 부여 여부도 문서 기준 이해만 갱신했지, 실제 운영 환경까지 확인한 건 아님
