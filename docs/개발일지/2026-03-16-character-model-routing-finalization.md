# 개발일지 - 캐릭터 모델 라우팅 최종안 문서화

- 작성시각: 2026-03-16 22:35:00 +09:00
- 해결하고자 한 문제:
  - 캐릭터별 모델 배정과 fallback 전략이 대화에서 정리됐지만 `docs/planning`에는 아직 최종안이 반영되지 않았음.
  - 특히 팀 내 4명이 서로 다른 기본 모델을 쓰고, 공통 fallback은 `Qwen: Qwen3.5-9B`, 최종 취합은 `Gemini`라는 기준을 문서에 고정할 필요가 있었음.
- 진행 내용:
  - `docs/planning/01-prd.md`에 캐릭터 모델 배정 원칙과 불리시/베어리시팀 모델 표를 추가함.
  - `docs/planning/05-api-spec.md`에 멀티 provider 배정표와 공통 fallback 정책을 반영함.
  - `docs/planning/06-tasks.md`의 provider 작업 항목을 현재 합의에 맞게 보정함.
- 해결된 것:
  - 불리시팀과 베어리시팀이 각각 서로 다른 4개 기본 모델을 쓰는 방향이 문서상 고정됨.
  - 공통 fallback이 `Qwen: Qwen3.5-9B`, 최종 결과 취합이 `Gemini`라는 기준이 planning 문서에 반영됨.
- 해결되지 않은 것:
  - 실제 코드 라우팅표는 이 최종안으로 별도 반영이 필요함.
